---
# cluster specific variables
username: enforge
passhash: yftK48L59TcL6
  # To generate crypt(3) hashes install "whois" and type mkpasswd
  # somepass: yftK48L59TcL6
# reminder: the ssh-key is more relevant, as only local login is enabled with this account (no ssh)
# Still, it is required for sudo commands after sshing

# debian specific variables
mirror: deb.debian.org
# recommended minimum:
  #       openssh-server wget curl git net-tools nano
packages: openssh-server wget curl git net-tools nano
...